#include <stdio.h>

int main() {
    int N;
    printf("Saisir N: ");
    scanf("%d",&N);

    float tab[N];

    for(i=0; i<N; i++)
        tab[N] = float tab[N];
    
    printf("Matrice générée.\n");
}